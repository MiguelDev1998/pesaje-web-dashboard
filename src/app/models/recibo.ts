export interface Recibo {
  recibo_id: number;
  numero_partida: number;
  fecha: string;
  codigo: string;
  proveedor: string;
  saco_nylon: number;
  saco_yute: number;
  peso_recibo: number;
  compra: string;
}
